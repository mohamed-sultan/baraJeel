import React, { PureComponent } from "react";
import { Text, View } from "react-native";

export default class ordersDone extends PureComponent {
  render() {
    return (
      <View>
        <Text> ordersDone </Text>
      </View>
    );
  }
}
